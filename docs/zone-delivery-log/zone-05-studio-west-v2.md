# Zone 05 studio-west delivery log

Absolute path: `/Users/admin/Prj/sunwood-lab-recreation/docs/zone-delivery-log/zone-05-studio-west-v2.md`.

The selected 3 columns × 2 rows v2 sheet holds six independently extracted
candidate sprites: irregular worktable, rear plant shelf, tall planter
bookshelf, blank ideas whiteboard, rolling chair, and monitor/device group.
The first v1 attempt is retained as intermediate evidence but is not a
candidate because its table and chair touched the right cell boundary. v2 uses
a 72-distance chroma threshold to account for image-generation color drift;
all six v2 alpha bounds have four false edge flags.

`IDEAS` / `IMPACT` remain deterministic overlays. Source-instance review and
runtime placement are open; this is not a direct crop of the reference image.

## AYANO delivery

Pending asset commit and push. The delivery must attach the v2 chroma master
and the private source-to-children comparison proof to `アヤノの部屋 /
#sunwood-lab-recreation` (`1526794078744744077`).
