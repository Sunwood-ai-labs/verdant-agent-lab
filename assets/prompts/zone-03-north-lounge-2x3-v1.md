# Zone 03 — north lounge, 2x3 sheet prompt v1

Use case: stylized-concept

Asset type: reusable HTML pixel-office sprites, generated as one landscape
two-column by three-row chroma-key source sheet.

Input image: the user-supplied office reference, specifically the north lounge
crop at original-image bbox `[826, 128, 1001, 272]`; use it only for material,
viewing-angle, palette, and silhouette guidance.

Primary request: exactly six separate non-character pixel-art objects in a
strict 3 columns × 2 rows sheet: (1) olive-green polygonal rug platform;
(2) long olive-green L-shaped sofa main section; (3) short olive-green right
sofa return; (4) low round warm-wood coffee table; (5) small tabletop plant;
(6) small tabletop book/card/tea props. Use warm detailed isometric pixel art
matching the reference.

Scene/backdrop: perfectly flat `#ff00ff` chroma-key background. Each object is
fully contained in one cell with broad empty gutters and padding. No grid lines.

Constraints: no characters or people; no floor, walls, garden, shadows,
reflection, watermark, logos, readable text, labels, UI, or extra objects.
Exactly six independently placeable objects; no `#ff00ff` inside an object.
