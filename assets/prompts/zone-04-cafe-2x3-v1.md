# Zone 04 — cafe, 2x3 sheet prompt v1

Use case: stylized-concept

Asset type: reusable HTML pixel-office sprites, one landscape 3 columns × 2
rows chroma-key source sheet.

Input image: the user-supplied office reference, specifically the north-east
cafe crop at original-image bbox `[1023, 36, 1213, 245]`; use only for its
warm isometric pixel style, material, palette, viewing angle, and silhouettes.

Primary request: exactly six separate non-character pixel-art objects, in
row-major order: (1) lower wooden cafe counter cabinet; (2) greenery window
fixture with plant pots; (3) left narrow glass-front refrigerator; (4) right
tall gray vending refrigerator; (5) group of three matching silver espresso
machines; (6) blank green cafe sign panel. The sign panel must have no letters.

Scene/backdrop: flat uniform `#ff00ff` chroma-key background. Each object stays
inside one cell with large entirely empty gutters and padding. No grid lines.

Constraints: no character/person; no floor, walls, adjacent shelves, shadows,
reflection, watermark, logo, readable text, labels, UI, or extra objects.
Exactly six independent assets; never use `#ff00ff` inside an object.
