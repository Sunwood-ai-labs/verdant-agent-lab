# Zone 05 — west studio, 2x3 sheet prompt v1

Use case: stylized-concept

Asset type: reusable HTML pixel-office sprites, one landscape 3 columns × 2
rows chroma-key source sheet.

Input: user-supplied office reference, west studio crop bbox `[194, 264, 563, 488]`,
for warm isometric pixel-art style, material, palette, and silhouettes only.

Create exactly six independent non-character assets, row-major: (1) irregular
continuous wooden worktable with empty surfaces; (2) low rear shelf with small
plants but no wall/floor; (3) tall right planter bookshelf; (4) blank framed
ideas whiteboard; (5) black rolling office chair; (6) grouped green monitor,
black diagonal monitor, and small tabletop devices. Whiteboard has no words or
drawings.

Use uniform `#ff00ff` chroma background. Each cell has broad empty gutters and
padding; no object touches a boundary. No grid lines.

No people, floor, walls, shadows, reflections, logo, readable text, labels,
watermark, UI, or extra objects. Exactly six crisp opaque pixel-art assets;
never use `#ff00ff` inside an object.
